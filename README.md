# mattermost-poc

A Go project template with formatting and linting tooling.

## Usage

```bash
make build    # build the project
make test     # run tests
make format   # run gofmt
make lint     # run linter
```
